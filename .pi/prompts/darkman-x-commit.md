---
description: Write the commit message for staged changes as Conventional Commits — darkman-x-commit mode.
---

Load `skills/darkman-x-commit/SKILL.md` (or `.pi/skills/darkman-x-commit/SKILL.md`). Look at the staged diff and write a commit message per that spec: `type(scope): subject` (≤50 chars), body only if it explains why or a breaking change. This is one-shot — your prose intensity for everything else in the conversation is unaffected.
