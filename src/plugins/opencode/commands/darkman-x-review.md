---
description: Review the current diff or file, one line per finding — darkman-x-review mode.
---

Load `skills/darkman-x-review/SKILL.md`. Review `$ARGUMENTS` (or the current diff if no argument given) and report findings as `L<line>: <problem>. <fix>.` with optional 🔴🟡🔵❓ severity. One-shot — your prose intensity for everything else in the conversation is unaffected.
