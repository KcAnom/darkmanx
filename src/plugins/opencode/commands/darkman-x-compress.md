---
description: Compress a markdown/text memory file's style while preserving code, URLs, and paths byte-exact.
---

Load `skills/darkman-x-compress/SKILL.md`. Run `python3 -m scripts $ARGUMENTS` from that skill's `scripts/` directory against the given file path. This is one-shot — your prose intensity for everything else in the conversation is unaffected.
